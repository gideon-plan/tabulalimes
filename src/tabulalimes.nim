## tabulalimes.nim -- Tabula + Limes document ingestion. Re-export module.
{.experimental: "strict_funcs".}
import tabulalimes/[extract, ingest, batch, lattice]
export extract, ingest, batch, lattice
